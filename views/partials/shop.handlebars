<section class="container mx-auto col-start-3 grid col-end-12 h-screen" id="oldNode">
    <div class="grid grid-cols-9 grid-rows-6">
        <!-- insert object here -->
        <div class="shopHeader col-start-1 col-end-10 row start 1 flex justify-center items-center m-4">Tool Shop</div>
        <div class="shopCards flex flex-wrap justify-center col-start-1 col-end-10 row-start-2 row-end-7">

            {{log tool}}
            {{#each tool as |shopItem|}}
            {{> shopcards}}
            {{/each}}

        </div>
    </div>
    </div>
</section>